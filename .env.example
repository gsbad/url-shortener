# Copie para .env e ajuste. O .env nunca deve ser commitado (ver .gitignore).

ENVIRONMENT=development
SERVER_PORT=8080

# Base usada para montar a shortUrl devolvida pela API.
APP_BASE_URL=http://localhost:8080

DB_HOST=localhost
DB_PORT=5432
DB_NAME=urlshortener
DB_USER=urlshortener
DB_PASSWORD=troque-me
