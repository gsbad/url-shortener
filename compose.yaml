# Compose para desenvolvimento local. Um serviço apenas.
# Adicionar banco só quando o projeto precisar — ver o bloco comentado.
services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      ENVIRONMENT: development
    # env_file: .env

  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: url-shortener
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-senha-local-de-dev}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  # volumes:
  #   pgdata:
